name: My first Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: welcome Message
              run: echo "My First CI/CD Pipline"

            - name: List file
              run: ls

            - name: Read File
              run: cat README.md
